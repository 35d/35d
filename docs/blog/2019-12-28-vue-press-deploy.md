---
title: VuePress + Netlify で GitHub に Push したら自動で公開されるように設定する
date: 2019-12-28
category: Development
tags:
  - VuePress
meta:
  - name: og:title
    content: VuePress + Netlify で GitHub に Push したら自動で公開されるように設定する
  - name: og:description
    content: このブログの制作過程も記事にしていこうと思います。Netlify の場合は GUI で設定ができるので、以下のように設定すれば完了でした。  
  - name: og:type
    content: article
---

# VuePress + Netlify で GitHub に Push したら自動で公開されるように設定する

表題の通りで、このブログの制作過程も記事にしていこうと思います。
[VuePress の公式ページ](https://vuepress.vuejs.org/guide/deploy.html)
に各ホスティングサービスごとのデプロイ方法がまとまっているので（メチャ親切）、そこを参考に設定していきました。

Netlify の場合は GUI で設定ができるので、以下のように設定すれば完了でした。  

![Netlify スクリーンショット](../assets/img/2019-12-28-1.png)

普段使ってるエディタでブログが書けて、コミット・プッシュしただけで公開されるので、執筆体験も最高です。😭